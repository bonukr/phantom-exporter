services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      H2H_PORT: "8080"
      H2H_SETTINGS_DIR: /app/settings
      H2H_LOG_FILE: /app/logs/phantom-exporter.log
      H2H_LOG_LEVEL: info
    volumes:
      - ./settings:/app/settings
      - ./logs:/app/logs
