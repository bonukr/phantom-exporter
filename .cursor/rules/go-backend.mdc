---
description: Go 백엔드 규칙 (gin, slog, PostgreSQL, 환경변수)
globs: **/*.go
alwaysApply: false
---

# Go 백엔드 규칙

## gin

- HTTP 라우팅/핸들러는 모두 gin 사용. `r := gin.New()` 후 Recovery + 커스텀 slog 미들웨어를 붙인다.
- 라우트 그룹 분리: `/api/...`(JSON CRUD), `/metrics`(Prometheus 텍스트 노출), `/`(HTML GUI).
- 핸들러는 얇게 유지하고 비즈니스 로직은 `internal/metrics`, `internal/store`로 위임한다.

## slog 파일 로그

- 표준 `log/slog`만 사용. 출력은 파일로 기록(예: `H2H_LOG_FILE` 환경변수, 기본 `./logs/phantom-exporter.log`).
- JSON 핸들러 권장. 요청 로그는 gin 미들웨어에서 method/path/status/latency를 구조화 필드로 남긴다.
- `fmt.Println`/`log.Print`로 로깅하지 말 것. 항상 slog 사용.

```go
logger := slog.New(slog.NewJSONHandler(logFile, &slog.HandlerOptions{Level: lvl}))
logger.Info("request", "method", c.Request.Method, "path", c.FullPath(), "status", c.Writer.Status())
```

## 설정 저장소 (settings 폴더)

- 메트릭 정의/설정값은 **settings 폴더의 그룹별 YAML 파일**에 저장하고 거기서 읽는다. 하드코딩 금지.
- 폴더 경로는 환경변수 `H2H_SETTINGS_DIR`(기본 `./settings`)로 받는다. 비어 있으면 기본값으로 자동 생성.
- 파일명은 그룹 path: `settings/<path>.yml`. 그룹 path 변경 시 파일도 rename(삭제+생성)한다.
- 파일 I/O는 `internal/store` 안에서만. 핸들러에서 직접 파일을 읽고 쓰지 않는다.
- 전체 그룹을 메모리에 두고 mutex로 보호하며, 변경 시 영향받는 그룹 파일만 temp + rename으로 원자적 저장.
- 저장소 메서드 시그니처는 `context.Context`를 유지한다(향후 백엔드 교체 대비).

## 에러 처리

- 에러는 `fmt.Errorf("...: %w", err)`로 래핑하고, 핸들러 경계에서 slog로 로깅 후 적절한 HTTP 상태 반환.
- 에러를 조용히 무시(`_ = err`)하지 않는다.
